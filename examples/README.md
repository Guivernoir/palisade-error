# Palisade Errors Examples

This directory contains examples demonstrating the core security features of the `palisade-errors` library.

## How to Run

From the root of the project:

```bash
cargo run --example basic_usage
cargo run --example honeypot_scenario
cargo run --example forensic_logging
cargo run --example secure_macros